# turfr-ingest

`turfr-ingest` converts human-written match records into validated canonical
match data that can be resolved and ingested into the Turfr domain.

## Canonical Format

The canonical match format defines the stable representation between a
human-written match record and the Turfr domain/database.

See [CANONICAL_FORMAT.md](./CANONICAL_FORMAT.md).